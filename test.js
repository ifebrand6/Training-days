let countString = '';
let i = 0;

do {
countString = countString + i;
i++;
} while (i < 5);

console.log(countString);

const delayed = () => { i + 2

};

const delay = function delay() {
    console.log(`this anwser is ${i}`);
};
delay();

function big(input) {
    input / 2
}
let bigOj = {
    okay: [],
    peopel: function big(input) {
        if (input > 0){
            return input / 2
        }
        else {
            console.log("bad input");
        }

    }
    
};
let test = bigOj.peopel(0)
console.log(test);